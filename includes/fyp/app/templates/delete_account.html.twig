{% extends 'layout.html.twig' %}
{% block title %} Delete Account - DM Friend {% endblock %}
{% block content %}
    <section id="delete_account">
        <h3>Deleting your account will remove all enemies, actions, and encounters you have created. These will never be accessible again. </h3>
        <h4>We're sad to see you go, but if you are sure:</h4>
        {% if delete_error %}
        <p class="error_message">Passwords don't match</p>
        {% endif %}

        <form action="{{ action }}" method="{{ method }}">
            <label for="password">Enter your password to delete your account.</label>
            <input id="password" name="password" type="password">
            <input type="submit" value="Confirm Delete">
        </form>
    </section>
{% endblock %}
